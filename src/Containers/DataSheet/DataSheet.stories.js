import React from 'react';
import { storiesOf } from '@storybook/react';
import DataSheet from './DataSheet';
storiesOf('DataSheet', module).add('DataSheet', () => (
  <div>
    <DataSheet />
  </div>
));
